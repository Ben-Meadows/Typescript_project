//so thing i need to remeber when working with node and typescript
//something about exports and stuff idk we will figure it out 

export function greet(name: string): string {
    return `Hello, ${name}!`;
  }